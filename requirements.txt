# =============================================================================
#  Kalshi 15-Minute Crypto Trading Bot - Windows Edition
#  CORE dependencies. Lightweight and REQUIRED: the bot, dashboard, Gemini
#  Captain and Kalshi signing all run on just these.
#
#  The heavy TimesFM / PyTorch forecasting model is OPTIONAL and lives in
#  requirements-optional.txt, so a PyTorch install failure can never block the
#  rest of the system. Without it the bot uses a built-in statistical
#  BaselineForecaster, which is plenty for paper trading.
# =============================================================================

# --- Core async networking / data -------------------------------------------
websockets>=12.0          # Kalshi + spot-feed realtime streams
httpx>=0.27.0             # Async REST (Kalshi v2 + exchange candle seeding)
numpy>=1.24.0             # Baseline forecaster math
python-dotenv>=1.0.0      # .env configuration loading (REQUIRED for API keys)

# --- Kalshi authentication --------------------------------------------------
cryptography>=42.0.0      # RSA-PSS request signing for Kalshi v2 API

# --- Dashboard ---------------------------------------------------------------
fastapi>=0.111.0          # REST + WebSocket server for the monitoring dashboard
uvicorn[standard]>=0.29.0 # ASGI server (Windows-compatible)

# --- AI Captain (Gemini) -----------------------------------------------------
google-generativeai>=0.8.0   # "Captain" oversight via Gemini (lightweight)
